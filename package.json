{
  "name": "goal-tracker-project",
  "version": "1.0.0",
  "description": "Goal Tracker Project with API, Frontend, and Documentation",
  "scripts": {
    "start:api": "cd server && npm run dev",
    "start:frontend": "cd goal-tracker && npm run dev",
    "start:swagger": "cd swagger-ui && npm start",
    "start": "concurrently \"npm run start:api\" \"npm run start:swagger\"",
    "install:all": "npm install && cd goal-tracker && npm install && cd ../server && npm install && cd ../swagger-ui && npm install && cd ..",
    "test": "cypress run",
    "test:open": "cypress open"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "cypress": "^13.6.0"
  }
}
